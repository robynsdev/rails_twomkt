<nav>
  <%= link_to 'Home', home_index_path %>

  <% if user_signed_in? %>
    <span class='nav-greeting'> <%= "Welcome, #{current_user.name}" %> </span>
    <%= link_to "Sign Out", destroy_user_session_path, method: "delete" %>
  <% else %>
    <%= link_to "Sign In", new_user_session_path %>
  <% end %>
</nav>